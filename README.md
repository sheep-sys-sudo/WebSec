# WebSec
## git简单使用
```
git init
git add .
git commit -m "success to add"
git remote add WebSec https://github.com/sheep-sys-sudo/WebSec.git
//git remote -v
git pull WebSec main --rebase
//git branch -r
//git branch -m master main
git push -u WebSec main
```